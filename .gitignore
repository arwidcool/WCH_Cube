node_modules/
src-tauri/target/

# A reference copy of the INSTALLED PlatformIO platform `ch32v`, dropped here by
# the human to read. `agents/history/round3/00_PROJECT.md` and the round-5 rules
# in `agents/README.md`: do not vendor the SDK into this repo - it is a PlatformIO
# package. Keeping a second 45 MB copy in git is how the two get to disagree.
# Read it on disk; cite the package, not this.
data/Pio Source/

__pycache__/
*.pyc

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
