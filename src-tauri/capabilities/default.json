{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the WCH_CubeMX window is allowed to do: talk to its own commands and open native file dialogs.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
