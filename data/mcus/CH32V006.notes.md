# CH32V006.yaml — extraction notes

Sources: CH32V006 Datasheet V2.0 (DS) and CH32V00X Reference Manual V1.4 (RM), both as
markdown under `data/sources/`.

## Verification status

**Second pass complete (2026-09-11, AGENT-1).** `tools/extract_remaps.py` re-derives every
RM 7.2.11 remap table from the source text with its own parser and diffs it against this
YAML: **232 pin assignments across 7 peripherals, 0 differences.** Re-run it after any
edit to a `remaps:` block:

```
python tools/extract_remaps.py     # 0 = identical, 1 = differences, 2 = RM unreadable
python tools/validate_mcu.py       # schema, pin existence, signal parity, I/O counts
```

The differ is checked against planted faults, not just against a passing file: mutating a
single pin in the YAML makes it report that pin and exit non-zero.

## Confidence

| Block | Source | Confidence | Still to spot-check against the PDF |
|---|---|---|---|
| Package pin numbers (5 packages) | DS Table 2-1-1 columns D8U7 / F8P7 / F8U7 / E8R / K8U7 | High — I/O counts now asserted against the DS model table by `validate_mcu.py` | QFN pin-1 corner & CCW direction (the drawings were garbled in the md) |
| Variant I/O counts 31/22/22/18/18/18/11 | DS §1 model table, lines 99–105 | High | — |
| Shorted pins PA1+PA6 (QSOP24), PD7+PA4 (QFN12/20, TSSOP20) | DS notes 3, 4 | High | — |
| Reset pin per package | DS note 5 | High | — |
| TIM1 remap (10 columns) | RM Table 7-8 | **High — machine-diffed** | — |
| TIM2 remap (8 columns) | RM Table 7-9-1 (the V002/004/005/006 twin, not 7-9-2) | **High — machine-diffed** | — |
| USART1 (10), USART2 (7), SPI (7), I2C (5) remaps | RM Tables 7-10, 7-11, 7-12, 7-13-1 | **High — machine-diffed** | — |
| ADC trigger remaps (ETRGREG, ETRGINJ) | RM Tables 7-14 / 7-15 | **High — machine-diffed** | — |
| EXTI line → pin | RM Table 6-2 + AFIO_EXTICR (RM 7.3.2.1) | High | — |
| DMA1 channel map | RM Table 8-2 **and** Figure 8-1 | High — two separate renderings in the source agree on every entry | — |
| Option byte RST_MODE | RM Table 18-4 (ch.18) | High | — |
| TIM2 complementary outputs on the CH3/CH4 pins | RM 12.3.9 | High for the mechanism, **Medium for the per-remap detail** — see below | Whether CH1N is truly absent in TIM2_RM=001 and 100 |
| ADC channels IN0–IN7, triggers | DS pin table + RM Tables 7-14/7-15 | High | — |
| TouchKey channels = ADC input pins | DS 1.4.11 (inferred mapping CHx = INx) | Medium | Confirm in RM ch.10 |
| OPA pin roles P0–P3, N0–N2, OUT0/1 | DS pin table | High | Polling channel set (3 vs 4) |
| Clock tree | DS 1.3, RM RCC_CFGR0 (HPRE, ADCPRE, MCO, PLLSRC) | High | ADC min 16 MHz (DS 3.x table says 16–48) |

## Things worth knowing before you edit this file

**The RM contradicts itself once, and the YAML follows the table.** RM 7.3.2.2, the
AFIO_PCFR1 bit description, spells USART2_RM=101 as `RTS/PA11`. There is no PA11 on this
family — port A stops at PA7. RM Table 7-11 gives `PA1` for the same cell, which is what
this file uses. Anyone re-reading the register prose will hit this; it is a typo in the
source, not a disagreement to resolve.

**TIM2 has complementary outputs and they cost you channels 3 and 4.** RM 12.3.9: "when
complementary outputs are used, channels 3 and 4 act as channel 1 and channel 2
complementary output channels". So CH1N sits on the CH3 pin and CH2N on the CH4 pin, and
there are no extra pins — which is why the `remaps:` block still lists only four signals
and the feature is modelled as a `Complementary Outputs` setting that claims CH3/CH4.
Asking for both plain CH3 output and CH1N is a genuine conflict and the engine flags it.

The Medium-confidence part: the AFIO_PCFR1 prose annotates most remaps as `CH3/CH1N/...`
but omits CH1N for TIM2_RM=001 and 100. That may be real or may be dropped text. The
current model does not encode a per-remap restriction, so it is permissive there.

**Pin 0 is the exposed pad, not a pin.** It is excluded from the I/O count. A pair of
internally shorted GPIOs is one physical pin and counts once. Both rules are enforced.

**Unquoted commas inside `{ }` silently corrupt this file.** YAML flow mappings treat a
comma as a field separator, so

```yaml
- { name: IN8 (Vrefint, internal) }        # WRONG: name becomes "IN8 (Vrefint"
- { name: "IN8 (Vrefint, internal)" }      # right
```

The file still parses, and the app keys per-setting state by choice name, so two choices
whose names both truncate to the same string silently become one. Four live instances of
this were found and fixed on 2026-09-11. `validate_mcu.py` now rejects any unknown key in
a choice, setting, or pin mapping, which is what catches it.

## Not modelled yet

- CH32V005 sub-variant (no TKEY, no TIM3) — waiting on `inherits:` semantics from AGENT-2.
- TouchKey register-level detail (RM ch.10); channel→pin mapping is inferred from the DS.
- Option bytes other than RST_MODE: START_MODE, STANDBYRST, IWDG_SW, RDPR, WPR (RM 18).
- 5 V tolerance per pin — **the CH32V006 DS does not state it per pin**, so there is
  nothing to extract. Do not infer it from other families.
